
export interface SceneInfoBindGroupOptions {

    layoutIndex: number;

    directionalLights: boolean;
    sampler: boolean;
    skyboxConvoluted: boolean;
    skyboxPrefiltered: boolean;
    brdfLUT: boolean;

}